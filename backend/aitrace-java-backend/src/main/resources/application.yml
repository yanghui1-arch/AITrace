spring:
  application:
    name: springboot

  datasource:
    url: jdbc:postgresql://localhost:5432/aitrace
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true